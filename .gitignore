# A settings file, which is one somebody writes for their own machine and not
# one this project ships. It is also the first thing the collector looks for,
# in the working directory, so one committed here would quietly change every
# run started from a checkout, the test suite's included.
nettail.conf

__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Everything under these two is a workspace's own state, machine-specific and
# not this project's to ship, except the launch configs, which are shared on
# purpose so a checkout comes with a working "start the collector" button in
# either editor.
.idea/*
!.idea/runConfigurations/
.vscode/*
!.vscode/launch.json

.pytest_cache/
.coverage

# Agent tooling scratch. A worktree is a checkout of this repository living
# inside it, which git would otherwise try to record as a nested repo, and
# settings.local.json is per-user. A shared .claude/settings.json is
# deliberately not ignored, so one can still be committed if ever wanted.
.claude/worktrees/
.claude/settings.local.json

# The agent instructions live in AGENTS.md, which is committed and is what
# every agent reads. CLAUDE.md is only the one-line pointer at it that Claude
# Code looks for, which is per-user scaffolding rather than part of the
# project, so it stays out of the repository.
CLAUDE.md
